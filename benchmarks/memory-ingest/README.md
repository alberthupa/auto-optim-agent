# memory-ingest benchmark

This directory will contain the fixed benchmark for the `memory-ingest` skill.

The benchmark should stay separate from the skill under optimization.

Expected contents:

- benchmark case definitions
- fixture inputs
- a thin benchmark runner
- scoring logic
