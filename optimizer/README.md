# optimizer

This directory will contain the thin experiment loop that:

1. establishes a baseline
2. asks the agent for a small skill change
3. runs the fixed benchmark
4. records the score
5. keeps or rejects the change

The implementation should stay file-based and easy to inspect.
